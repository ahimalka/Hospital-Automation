[pytest]
# 1. Force strict mode to stop background loops
asyncio_mode = strict

# 2. Add this flag to prevent the 'Runner.run' conflict during teardown
addopts = --alluredir=allure-results -p no:asyncio